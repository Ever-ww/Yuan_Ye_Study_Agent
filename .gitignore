# 本机凭据与当前部署专用的模型设置，不应提交到版本库。
config.ini
model_choice/.env
.env
.env.*
!.env.example
.agents/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.codex

# 本地数据库、构建结果和 IDE 元数据均不属于可共享源码。
*.db
*.sqlite
*.sqlite3
build/
dist/
*.egg-info/
node_modules/
ui/dist/
ui/*.tsbuildinfo
desktop/src-tauri/target/
desktop/src-tauri/gen/
desktop/src-tauri/binaries/*
!desktop/src-tauri/binaries/.gitkeep
.idea/
.vscode/

# Yuan Ye 的全部本机配置、记忆和运行状态均由初始化命令创建，不提交。
.yy/
AGENT.md
AGENT.local.md
DEVELOPMENT_REFERENCE.md

# 已安装第三方 Skill 只保留在本机；目录占位文件用于保留结构。
skills/*
!skills/.gitkeep

# Chat 错误复现快照包含完整用户上下文，仅保留本机说明文件。
tests/error/*.jsonl
tests/error/*.log

# Python 字节码缓存。
__pycache__/
*.py[cod]

.VSCodeCounter
